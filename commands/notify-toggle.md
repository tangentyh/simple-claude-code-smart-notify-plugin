---
description: Toggle notification mode between smart (focus-aware) and always
allowed-tools: Bash
---

# Toggle Notification Mode

Run this command to toggle between smart and always notification modes:

```bash
bash "$(dirname "$(dirname "$0")")/scripts/toggle.sh"
```
